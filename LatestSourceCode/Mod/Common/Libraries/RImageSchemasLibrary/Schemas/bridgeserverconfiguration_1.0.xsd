<?xml version="1.0" encoding="UTF-8" ?>
<!--W3C Schema generated by XMLSpy v2005 rel. 3 U (http://www.altova.com)-->
<!--Please add namespace attributes, a targetNamespace attribute and import elements according to your requirements-->
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns:msdata="urn:schemas-microsoft-com:xml-msdata"
	elementFormDefault="qualified">
	<xs:import namespace="http://www.w3.org/XML/1998/namespace" />
	<xs:element name="BridgeServerConfiguration" msdata:IsDataSet="true">
		<xs:complexType>
			<xs:sequence>
				<xs:element ref="ServerInfo" />
				<xs:element ref="ImagerInfo" />
				<xs:element ref="ProducerInfo" />
			</xs:sequence>
		</xs:complexType>
	</xs:element>
	<xs:element name="ServerInfo">
		<xs:complexType>
			<xs:attribute name="ID" use="required" />
			<xs:attribute name="SoftwareVersion" use="required" />
			<xs:attribute name="Description" />
			<xs:attribute name="Hostname" use="required" />
		</xs:complexType>
	</xs:element>
	<!--
         ID in ServerInfo is used to connect to the messaging system, whereas the ID in either the
        ImagerInfo or ProdcerInfo element is used as the "Client ID" when submitting orders.  FileID is
        used to mark the"Server ID field in the file protocol and must be limited to four chars.
    -->
	<!--
         If MakeOrderIDUnique is not provided "Yes" is assumed, if "No" is provided all of the file
        protocol clients (of any file type) must assure that OrderIDs are unique across the whole
        system rather than just the protocol file.
    -->
	<!--
         Bridge Server incudes support for both Imager and Producer Protocol files.  The
        ImagerInfo and ProducerInfo elements define extra attributes for these pseudo servers.
    -->
	<xs:element name="ImagerInfo">
		<xs:complexType>
			<xs:attribute name="ImagerID" use="required" />
			<xs:attribute name="ImagerCluster" use="required" />
			<xs:attribute name="ImageOrderFile" use="required" />
		</xs:complexType>
	</xs:element>
	<xs:element name="ProducerInfo">
		<xs:complexType>
			<xs:attribute name="ProducerID" use="required" />
			<xs:attribute name="ProducerCluster" use="required" />
			<xs:attribute name="ProductionOrderFile" use="required" />
		</xs:complexType>
	</xs:element>
</xs:schema>