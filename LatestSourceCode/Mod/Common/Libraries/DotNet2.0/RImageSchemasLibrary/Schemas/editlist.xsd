<?xml version="1.0" encoding="UTF-8" ?>
<!--W3C Schema generated by XMLSpy v2005 rel. 3 U (http://www.altova.com)-->
<!--Please add namespace attributes, a targetNamespace attribute and import elements according to your requirements-->
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns:msdata="urn:schemas-microsoft-com:xml-msdata"
	elementFormDefault="qualified">
	<xs:import namespace="http://www.w3.org/XML/1998/namespace" />
	<!-- BasePath is the root on the source drive for files targeted to the CD -->
	<!-- BPath is the drive of UNC path to the source root -->
	<!-- SystemDst is the where the files will end up, PC, Mac or both -->
	<!-- CDDir is the CD directory, add these to set the default CD folder for files -->
	<xs:element name="EditList" msdata:IsDataSet="true">
		<xs:complexType>
			<xs:sequence>
				<xs:element maxOccurs="unbounded" ref="BasePath" />
			</xs:sequence>
			<xs:attribute name="Version" default="1.0.0.0" />
		</xs:complexType>
	</xs:element>
	<xs:element name="BasePath">
		<xs:complexType>
			<xs:sequence>
				<xs:element maxOccurs="unbounded" ref="CDPath" />
			</xs:sequence>
			<xs:attribute name="BPath" use="required" />
			<xs:attribute name="SystemDst" default="BOTH">
				<xs:simpleType>
					<xs:restriction base="xs:NMTOKEN">
						<xs:enumeration value="PC" />
						<xs:enumeration value="MAC" />
						<xs:enumeration value="BOTH" />
					</xs:restriction>
				</xs:simpleType>
			</xs:attribute>
		</xs:complexType>
	</xs:element>
	<xs:element name="CDPath">
		<xs:complexType>
			<xs:sequence>
				<xs:element maxOccurs="unbounded" ref="DstFile" />
			</xs:sequence>
			<xs:attribute name="CDDir" default="/" />
		</xs:complexType>
	</xs:element>
	<xs:element name="DstFile">
		<xs:complexType>
			<xs:attribute name="DstFileName" use="required" />
			<xs:attribute name="SourceOverride" />
		</xs:complexType>
	</xs:element>
</xs:schema>
<!-- FileSpec may contain DOS style wildcards. -->
<!-- DstFileName is the cd filename, and with no sourceoverride, is appended to basepath for the source -->
<!-- SourceOverride gets the file and puts it in DstFileName on the CD -->
<!-- If DstFileName has a fully qualified path it will ignore the basepath -->